# Pinned versions known to work with macOS + Python 3.11 for this project
numpy==2.2.6
opencv-python==4.12.0.88
mediapipe==0.10.14

# If you need the full dependency freeze, run:
#   .venv/bin/python -m pip freeze > requirements-full.txt
